#include "csmith.h"
static uint8_t c[] = {1};
void a();
long b(int h) {
  long d = 0;
  if (h < 0)
    return -1;
  int e = 0;
  for (int f = 0; f <= h; f++) {
    e++;
    d += f;
  }
  long a = d / e;
  return a;
}
void g();
uint8_t i() {
  uint16_t k[4];
  int f;
  for (; g + f < 4; f++)
    k[b(c[0] - 1)] = 5;
  for (; f; a(k))
    ;
  uint8_t *l[3][3], j;
  l[0][j] = c;
}
